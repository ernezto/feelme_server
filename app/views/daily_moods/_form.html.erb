<%= form_for @daily_mood, url: create_daily_moods_path do |f| %>
  <% if @daily_mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_mood.errors.count, "error") %> prohibited this daily_mood from being saved:</h2>

      <ul>
      <% @daily_mood.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="row-fluid well">
  <div class="span6">
    <div class="control-group">
      <%= f.label :unhappy_count, class: 'control-label' %>
      <%= f.text_field :unhappy_count, class: 'controls' %>
    </div>
    <div class="control-group">
      <%= f.label :happy_count, class: 'control-label'  %>
      <%= f.text_field :happy_count, class: 'controls'  %>
    </div>
    <div class="control-group">
      <%= f.label :date, class: 'control-label'  %>
      <%= f.date_field :date %>
    </div>
    <%=f.submit(:class=>'btn btn-primary')%>
  </div>
  <div class="span6">
  </div>
<div/>

<% end %>
